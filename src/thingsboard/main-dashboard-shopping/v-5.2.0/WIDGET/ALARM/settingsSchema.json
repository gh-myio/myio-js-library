{
  "schema": {
    "type": "object",
    "title": "Alarm Widget Settings",
    "properties": {
      "labelWidget": {
        "type": "string",
        "title": "Widget Label",
        "default": "Alarmes e Notificações"
      },
      "defaultTab": {
        "type": "string",
        "title": "Default Tab",
        "enum": ["list", "dashboard"],
        "default": "list"
      },
      "showCustomerName": {
        "type": "boolean",
        "title": "Show Customer Name on Alarm Cards",
        "default": false,
        "description": "Redundant on single-shopping dashboards — enable only for cross-customer contexts."
      },
      "refreshIntervalSeconds": {
        "type": "number",
        "title": "Auto-Refresh Interval (seconds, 0 = disabled)",
        "default": 180
      },
      "cacheIntervalSeconds": {
        "type": "number",
        "title": "Cache TTL (seconds)",
        "default": 180,
        "description": "How long alarm list results are cached before a new API call is made."
      },
      "maxAlarmsVisible": {
        "type": "number",
        "title": "Alarms Page Size (per fetch, auto-paginates)",
        "default": 100
      },
      "alarmsApiBaseUrl": {
        "type": "string",
        "title": "Alarms API Base URL",
        "default": "https://alarms-api.a.myio-bas.com",
        "description": "Inherited from MAIN_VIEW orchestrator. Override here only if needed."
      },
      "alarmsApiKey": {
        "type": "string",
        "title": "Alarms API Key",
        "default": "gcdr_cust_tb_integration_key_2026",
        "description": "Inherited from MAIN_VIEW orchestrator. Override here only if needed."
      },
      "enableDebugMode": {
        "type": "boolean",
        "title": "Enable Debug Mode",
        "default": false
      }
    },
    "required": []
  },
  "form": [
    "labelWidget",
    "defaultTab",
    "showCustomerName",
    "refreshIntervalSeconds",
    "cacheIntervalSeconds",
    "maxAlarmsVisible",
    "alarmsApiBaseUrl",
    "alarmsApiKey",
    "enableDebugMode"
  ]
}
