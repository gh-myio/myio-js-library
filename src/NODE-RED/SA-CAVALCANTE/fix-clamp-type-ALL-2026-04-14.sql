-- ===========================================================================
-- Fix: sincronizar clamp_type com config.config_clamp.value
-- Cliente: Sá Cavalcante — todas as centrais
-- Data: 2026-04-14
-- Total divergências: 112 slaves
-- ===========================================================================
-- ATENÇÃO: cada central tem seu próprio banco PostgreSQL (produto).
-- Conectar na central correta antes de executar cada bloco.
-- ===========================================================================


-- ---------------------------------------------------------------------------
-- CENTRAL: MESTRE-ALVARO-L0L1
-- Divergências: 15
-- ---------------------------------------------------------------------------
UPDATE slaves
SET clamp_type = (config -> 'config_clamp' ->> 'value')::int
WHERE id IN (2, 8, 9, 14, 19, 34, 46, 66, 71, 74, 86, 88, 92, 95, 99);


-- ---------------------------------------------------------------------------
-- CENTRAL: MESTRE-ALVARO-L2AC
-- Divergências: 4
-- ---------------------------------------------------------------------------
UPDATE slaves
SET clamp_type = (config -> 'config_clamp' ->> 'value')::int
WHERE id IN (21, 127, 128, 129);


-- ---------------------------------------------------------------------------
-- CENTRAL: MESTRE-ALVARO-L3L4
-- Divergências: 3
-- ---------------------------------------------------------------------------
UPDATE slaves
SET clamp_type = (config -> 'config_clamp' ->> 'value')::int
WHERE id IN (2, 40, 52);


-- ---------------------------------------------------------------------------
-- CENTRAL: MOXUARA
-- Divergências: 18
-- ---------------------------------------------------------------------------
UPDATE slaves
SET clamp_type = (config -> 'config_clamp' ->> 'value')::int
WHERE id IN (3, 62, 67, 85, 93, 98, 99, 100, 103, 105, 111, 121, 189, 193, 194, 199, 210, 218);


-- ---------------------------------------------------------------------------
-- CENTRAL: MONTSERRAT
-- Divergências: 3
-- ---------------------------------------------------------------------------
UPDATE slaves
SET clamp_type = (config -> 'config_clamp' ->> 'value')::int
WHERE id IN (61, 140, 175);


-- ---------------------------------------------------------------------------
-- CENTRAL: SHOPPING-DA-ILHA
-- Divergências: 17
-- ---------------------------------------------------------------------------
UPDATE slaves
SET clamp_type = (config -> 'config_clamp' ->> 'value')::int
WHERE id IN (38, 134, 151, 153, 159, 178, 196, 200, 215, 217, 275, 276, 277, 278, 283, 411, 417);


-- ---------------------------------------------------------------------------
-- CENTRAL: RIO-POTY
-- Divergências: 21
-- ---------------------------------------------------------------------------
UPDATE slaves
SET clamp_type = (config -> 'config_clamp' ->> 'value')::int
WHERE id IN (7, 114, 142, 153, 164, 186, 187, 196, 197, 206, 207, 208, 210, 211, 228, 229, 235, 240, 242, 345, 348);


-- ---------------------------------------------------------------------------
-- CENTRAL: ANANINDEUA
-- Divergências: 31
-- ---------------------------------------------------------------------------
UPDATE slaves
SET clamp_type = (config -> 'config_clamp' ->> 'value')::int
WHERE id IN (1, 9, 16, 50, 87, 91, 102, 111, 113, 140, 142, 148, 149, 153, 154, 166, 194, 195, 196, 201, 208, 209, 210, 211, 212, 213, 218, 219, 220, 233, 237);
