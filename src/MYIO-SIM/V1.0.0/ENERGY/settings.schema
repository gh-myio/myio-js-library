{
  "schema": {
    "type": "object",
    "title": "Settings",
    "properties": {
      "customerId": {
        "type": "string",
        "title": "customerId"
      }
    },
    "required": ["customerId"]
  }
}
