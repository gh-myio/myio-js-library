{
  "schema": {
    "type": "object",
    "title": "Settings",
    "properties": {
      "clientId": {
        "type": "string",
        "title": "clientId"
      },
      "clientSecret": {
        "type": "string",
        "title": "secretid"
      }
    },
    "required": ["clientId", "clientSecret"]
  }
}
